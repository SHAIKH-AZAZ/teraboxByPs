{
  "name": "terabox-dl-ts",
  "version": "1.0.0",
  "type": "module",
  "description": "TeraBox Downloader API",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "render-build": "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install",
    "render-start": "~/.bun/bin/bun src/index.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.0.0"
  }
}